Hello Jules,

I need you to implement a task queue system in my "En" repository on the "dl" branch. This repository contains a mega downloader bot, and the goal is to manage concurrent downloads and uploads to prevent overloading the server.

Here are the detailed requirements:

**1. Core Functionality:**

The queue system should have the following features:

*   **Separate Queues:** Implement two separate queues: one for downloads and one for uploads.
*   **Configurable Limits:** The queue limits should be configurable in the `config.py` file. Please add the following variables to `config_sample.py` with a default value of 0 (disabled):
    *   `QUEUE_ALL`: The maximum number of total active tasks (downloads + uploads).
    *   `QUEUE_DOWNLOAD`: The maximum number of concurrent downloads.
    *   `QUEUE_UPLOAD`: The maximum number of concurrent uploads.
*   **Queueing Logic:**
    *   When a new download or upload task is initiated, the system should check the current number of active tasks against the configured limits.
    *   If the limits are reached, the task should be added to the appropriate queue (download or upload) and wait for a slot to become available.
    *   If the limits have not been reached, the task should start immediately.
*   **Queue Processing:** The system should automatically process the queues and start the next waiting task as soon as a slot becomes free.
*   **Concurrency Control:** Use `asyncio.Lock` to ensure that the queue operations are thread-safe.

**2. `/forcestart` Command:**

*   Create a new command `/forcestart` that allows a user to bypass the queue for a specific task.
*   The command should be able to identify the task either by replying to the message that initiated the task or by providing a unique task ID.
*   This command should immediately start the specified task, regardless of the current queue limits.

**3. Implementation Guidance:**

*   **Task Manager:** Create a new file, `bot/helper/ext_utils/task_manager.py`, to house the core queueing logic (checking limits, adding to queues, processing queues).
*   **Integration:**
    *   Integrate the queue check from the `task_manager.py` into the existing download and upload process. This will likely be in the module that handles the `mega.nz` links.
    *   You will need to call the queue check function before a download or upload begins.
*   **Command Handler:** Create a new module, `bot/modules/force_start.py`, to implement the logic for the `/forcestart` command.
*   **Handlers:** Add the necessary handlers for the new `/forcestart` command in the appropriate file (e.g., `bot/core/handlers.py`).

**4. Example Workflow:**

1.  A user initiates a download.
2.  The bot checks the queue limits in `task_manager.py`.
3.  If the download queue is full, the task is put on hold.
4.  When another download completes, the bot's `task_manager.py` automatically starts the waiting task.
5.  If a user wants to prioritize a queued task, they can use the `/forcestart` command.

Please follow these instructions to implement the queue system in the "En" repository on the "dl" branch. Let me know if you have any questions.